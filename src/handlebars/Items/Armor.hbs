---
NoteType: Armor
aliases:
tags:
Source: {{md-yamlText system.publication.title}}
Remaster: {{system.publication.remaster}}
Level: "{{system.level.value}}"
Rarity: {{md-titleCase system.traits.rarity}}
Traits:
  {{#each system.traits.value}}
  - "{{md-trait this}}"
  {{/each}}
Category: {{md-titleCase system.category}}
Group: {{md-titleCase system.group}}
Price: {{md-coinLabel system.price.value}}
ACBonus: {{system.acBonus}}
DexCap: {{system.dexCap}}
CheckPenalty: {{system.checkPenalty}}
SpeedPenalty: {{system.speedPenalty}} ft
Strength: {{system.strength}}
Bulk: {{system.bulk.value}}
---
# {{name}}
## Traits:`VIEW[{Traits}][text(renderMarkdown)]`
### **Price:**`VIEW[{Price}][text(renderMarkdown)]`; **AC Bonus:**`VIEW[{ACBonus}][text(renderMarkdown)]`; **Dex Cap:**`VIEW[{DexCap}][text(renderMarkdown)]`; **Check Penalty:**`VIEW[{CheckPenalty}][text(renderMarkdown)]`; **Speed Penalty:** `VIEW[{SpeedPenalty}][text(renderMarkdown)]`;
### **Strength:** `VIEW[{Strength}][text(renderMarkdown)]`; **Bulk:** `VIEW[{Bulk}][text(renderMarkdown)]`; **Category:** `VIEW[{Category}][text(renderMarkdown)]`; **Group:** `VIEW[{Group}][text(renderMarkdown)]`;
***
{{md-HTMLtoMarkdown system.description.value}}
